{
  "name": "hugging-xberg-mcp",
  "version": "2.0.0",
  "description": "Wrapper MCP server for Xberg — exposes extract_bytes and extract_structured locally, delegates to remote Xberg via MCP",
  "type": "module",
  "main": "src/mcp-server.mjs",
  "scripts": {
    "start": "node src/mcp-server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.4.3"
  }
}